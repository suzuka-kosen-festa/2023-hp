import RightCloud from "./right-cloud";
import type { Meta, StoryObj } from "@storybook/react";

const meta: Meta<typeof RightCloud> = {
  component: RightCloud,
  tags: ["autodocs"],
};

type Story = StoryObj<typeof RightCloud>;

export const Default: Story = {};

export default meta;
